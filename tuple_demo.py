coordinates = (4,5) #used specifically for coordinated, things that do not change
#list of tuples
#coordinates = [(4,5), (6,7), (80,34)]
#tuple cannot be modified it is unique
print(coordinates[0])
coordinates[1] = 20
print(coordinates[1])
#differece b/w tuple and list
# you can mutate/modify the list but tuples cannot be changed